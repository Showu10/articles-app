<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= article.title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <a href="/notes">← Go Back</a>
    <h1><%= article.title %></h1>
    <p><%- article.article.replace(/\n/g, '<br>') %></p>

    <form action="/notes/<%= article._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete</button>
    </form>

    <h2>Edit Note</h>
    <form action="/notes/<%= article._id %>?_method=PUT" method="POST">
        <input type="text" name="title" value="<%= article.title %>" required>
        <textarea type="text" name="article" required><%= article.article %></textarea>
        <button type="submit">Update</button>
    </form>
</body>
</html>